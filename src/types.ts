export interface IHeaderTitleConfig {
    title: string;
    route: string;
    id: number;
}

export interface IHomePicConfig {
    image: string;
    title: string;
    price: number;
    discount: number;
    hasPicture: boolean;
}
